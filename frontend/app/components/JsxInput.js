import {Border} from "@/app/components/Border";

export default function JsxInput({element})
{
    return (<div>
        Main JSX Input
        <Border>
            {element}
        </Border>
    </div>)
}