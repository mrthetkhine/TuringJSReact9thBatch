import { Metadata } from "next";

export default function DashboardPage()
{
    //throw new Error('Dshboard got error');
    return (<div>
        Parallel home page
    </div>);
}
export const metadata: Metadata = {
    title: 'Dashboard',
}